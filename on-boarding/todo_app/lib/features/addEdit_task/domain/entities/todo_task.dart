import "task.dart";
import 'package:meta/meta.dart';

class ToDoTask extends Task {
  ToDoTask(title, description, dueDate, isDone)
      : super(title, description, dueDate, isDone);

  List ToDoTasks(Task task) {
    List tasks = [];
    return tasks;
  }
}
